version: '3.1'

services:

  angular-front:
    container_name: angular-front
    build:
      context: ./front
      dockerfile: Dockerfile
    volumes:
      - ./front:/app
      - /app/node_modules
    ports:
      - 4200:4200

  spring-orm:
    container_name: spring-boot-swagger-docker
    build:
      context: ./orm
      dockerfile: Dockerfile
    ports:
      - 8080:8080